import { Box, Typography } from '@mui/material';
import { ReactElement } from 'react';

export default function ShopSidebar(): ReactElement {
  return (
    <Box>
      <Typography>Hello</Typography>
    </Box>
  );
}
